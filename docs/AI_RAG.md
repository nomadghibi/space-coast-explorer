# AI And RAG

AI starts in M5 with provider abstractions, source attribution, prompt-injection protections, and pgvector retrieval. M0 only creates the package boundary.

M5 foundation defines typed evidence and answer contracts, a provider interface, and deterministic input screening. No answer is grounded without evidence IDs and canonical source URLs. Retrieved web text is data only and cannot modify instructions, permissions, or tool access.
